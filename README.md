# Tokenized Protocol Explorer

## Install

In this directory run:
```
git clone https://github.com/tokenized/specification
npm install
```

## Build and deploy

```
npm run build
```

Then copy the contents of the `dist` directory to the web.